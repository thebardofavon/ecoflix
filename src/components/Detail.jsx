import styled from "styled-components";

const Details = (props) => {
    return (
        <Container>
            <h1>DETAILS</h1>
        </Container>
    )
};

const Container = styled.div``;

export default Details;